rp:
  amqp:
    host: localhost
    user: rabbitmq
    pass: rabbitmq
    apiuser: rabbitmq
    apipass: rabbitmq
    port: 5672
    apiport: 15672
  db:
    user: rpuser
    pass: rppass
    host: localhost
  plugins:
    rootDir: D:\IdeaProjects\ReportPortal\service-api\plguins
  datasource:
    maximumPoolSize: 12

datastore:
  type: minio
  accessKey: minio
  secretKey: minio123
  endpoint: http://localhost:9000
  bucketPrefix: prj-
  bucketPostfix:
  defaultBucketName: rp-bucket