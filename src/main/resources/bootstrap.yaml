info:
  build:
    name: API Service
    description: ReportPortal API Service
    version: '@version@'
    branch: '@branch@'
    repos: '@repos@'

spring:
  profiles:
    ## Profiles list
    active: ${rp.profiles:default}
  application:
    name: api
