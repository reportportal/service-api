spring:
  cloud:
    consul:
      discovery:
        hostname: docker.for.win.localhost
      host: localhost
rp:
  uat:
    serviceUrl: http://localhost:8080/uat
  analyzer:
    url: http://localhost:8080/analyzer
  db:
    url: jdbc:postgresql://localhost:5432/reportportal?user=postgres&password=root
  amqp:
    url: amqp://guest:guest@localhost:5672