info:
  build:
    name: API Service
    description: ReportPortal API Service
    version: '@version@'
    branch: '@branch@'

spring:
  profiles:
    ## Default one is still Eureka
    active: ${rp.profiles:eureka}
  application:
    name: api
