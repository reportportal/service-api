apply plugin: 'com.github.spotbugs'

spotbugs {
    toolVersion = '3.1.12'
    sourceSets = [sourceSets.main]
    effort = 'max'
    reportLevel = 'high'
}

spotbugsMain {
    reports {
        html.enabled(true)
        xml.enabled(false)
    }
}
