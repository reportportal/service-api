rp:
  binarystore:
    type: filesystem
    path: ${java.io.tmpdir}
  plugins:
    rooDir: ${java.io.tmpdir}
logging:
  file: ${java.io.tmpdir}/${spring.application.name}.log
embedded:
  datasource:
    dir: /embedded-postgres
    clean: true
    port: 0