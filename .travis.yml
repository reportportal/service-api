language: java

dist: trusty
sudo: required
script: ./gradlew build --info

env:
  global:
  - GRADLE_OPTS="-Xms128m -Xmx1g -Dorg.gradle.parallel=false -Dorg.gradle.daemon=false"

jdk:
  - oraclejdk8

notifications:
  slack: reportportal-dev:fl6xWHVQp1jvsMmCJxYW9YKP

cache:
  directories:
  - $HOME/.m2
  - $HOME/.gradle

after_success:
  - bash <(curl -s https://codecov.io/bash)
