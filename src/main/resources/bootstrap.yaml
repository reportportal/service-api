info:
  build:
    name: API Service
    description: ReportPortal API Service
    version: '@version@'
    branch: '@branch@'
    repo: '@repo@'

spring:
  profiles:
    ## Profiles list
    active: consul, dev-mac
  application:
    name: api
