rp:
  amqp:
    host: localhost
  db:
    host: localhost
  datasource:
    maximumPoolSize: 5
  plugins:
    rootDir: plugins
  binarystore:
    path: data\storage

logging:
  level:
    org:
      hibernate:
        SQL: debug