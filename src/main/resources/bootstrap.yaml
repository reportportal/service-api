info:
  build:
    name: API Service
    description: ReportPortal API Service
    version: '@version@'
    branch: '@branch@'

spring:
  profiles:
    ## Profiles list
    active: ${rp.profiles:}
  application:
    name: api
