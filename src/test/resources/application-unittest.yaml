rp:
  binarystore:
    type: filesystem
    path: ${java.io.tmpdir}
embedded:
  datasource:
    dir: /embedded-postgres
    clean: true
    port: 0
logging:
  file: ${java.io.tmpdir}/${spring.application.name}.log