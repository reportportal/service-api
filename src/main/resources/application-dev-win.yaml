rp:
  amqp:
    host: localhost
  db:
    host: localhost
  plugins:
    rootDir: plugins
  datasource:
    maximumPoolSize: 5
logging:
  level:
    org:
      hibernate:
        SQL: debug
server:
  servlet:
    context-path: '/api'